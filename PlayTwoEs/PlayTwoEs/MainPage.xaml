<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PlayTwoEs"
             x:Class="PlayTwoEs.MainPage">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <Label Text="Welcome to Play 2 Es!"
         VerticalOptions="Center"
         HorizontalOptions="Center"
         Grid.Row="0" 
         Grid.Column="0"/>
    
    <Button Text="Listen to Music"
          HorizontalOptions="Center"
          VerticalOptions="Center"
          Clicked="ListenToMusic"
            Grid.Row="1" Grid.Column="0"
          />

  </Grid>

</ContentPage>